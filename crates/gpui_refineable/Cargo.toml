[package]
name = "mdrv-gpui-refineable"
version = "0.0.260925"
edition = "2024"
license = "Apache-2.0"
publish = true
description = "Refineable trait + cascade for gpui-ce (vendored from Zed)."

[lib]
name = "refineable"
path = "src/refineable.rs"
doctest = false

[dependencies]
derive_refineable = { package = "mdrv-gpui-derive-refineable", version = "0.0.260925", path = "../gpui_derive_refineable" }
