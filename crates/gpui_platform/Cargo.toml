[package]
name = "gpui_ce_platform"
version = "0.1.0"
description.workspace = true
edition.workspace = true
publish.workspace = true
license = "Apache-2.0"

[lints]
workspace = true

[lib]
path = "src/gpui_platform.rs"

[features]
default = []
bench-support = [
  "gpui/bench-support",
  "gpui_apple/bench-support",
  "gpui_macos/bench-support",
]
font-kit = ["gpui_macos/font-kit"]
test-support = [
  "gpui/test-support",
  "gpui_apple/test-support",
  "gpui_macos/test-support",
  "gpui_windows/test-support",
]
screen-capture = [
  "gpui/screen-capture",
  "gpui_macos/screen-capture",
  "gpui_windows/screen-capture",
]
wayland = ["gpui_linux/wayland"]
x11 = ["gpui_linux/x11"]

[dependencies]
gpui.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
gpui_apple.workspace = true
gpui_macos.workspace = true

[target.'cfg(target_os = "windows")'.dependencies]
gpui_windows.workspace = true
gpui = { workspace = true, features = ["windows-manifest"] }

[target.'cfg(any(target_os = "linux", target_os = "freebsd"))'.dependencies]
gpui_linux.workspace = true

[target.'cfg(target_family = "wasm")'.dependencies]
gpui_web = { path = "../gpui_web", version = "0.1.0", package = "gpui_ce_web", default-features = false }
console_error_panic_hook.workspace = true
